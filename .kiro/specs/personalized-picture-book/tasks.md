# 実装計画: パーソナライズ絵本作成アプリケーション

## 概要

React + TypeScript フロントエンドと Express + TypeScript バックエンドで構成されるパーソナライズ絵本作成アプリケーションの実装計画。共有型定義・バリデーションスキーマから始め、バックエンド API、フロントエンド UI の順に段階的に構築する。各ステップで前のステップの成果物を活用し、孤立したコードが残らないようにする。

## タスク

- [x] 1. プロジェクト構造と共有型定義のセットアップ
  - [x] 1.1 モノレポ構造の作成と依存関係のインストール
    - `packages/shared/`, `packages/backend/`, `packages/frontend/` のディレクトリ構造を作成
    - 各パッケージの `package.json`, `tsconfig.json` を設定
    - 共通の依存関係（zod, vitest, fast-check）をインストール
    - _要件: 全体_

  - [x] 1.2 共有型定義と Zod バリデーションスキーマの実装
    - `packages/shared/src/types.ts` に `Theme`, `BookStatus`, `PageData`, `BookData`, `ProgressEvent` 等の型定義を作成
    - `packages/shared/src/schemas.ts` に `CreateProfileSchema`, `GenerateBookSchema`, `UpdatePageSchema`, `RegisterSchema`, `LoginSchema` を実装
    - `packages/shared/src/constants.ts` に `THEME_LABELS` 等の定数を定義
    - _要件: 1.1, 1.3, 1.4, 1.5, 2.1, 6.4_

  - [x] 1.3 プロパティテスト: プロフィールバリデーション (Property 1, 2)
    - **Property 1: プロフィール保存ラウンドトリップ** — 有効なプロフィールデータが CreateProfileSchema でパースした後、元のデータと一致することを検証
    - **Property 2: プロフィールバリデーションによる無効データの拒否** — 無効なプロフィールデータ（名前空文字、年齢範囲外、名前50文字超）が拒否されることを検証
    - **検証対象: 要件 1.2, 1.3, 1.4, 1.5**

- [x] 2. バックエンド認証機能の実装
  - [x] 2.1 Express サーバーと認証 API の実装
    - `packages/backend/src/app.ts` に Express アプリケーションを作成
    - `packages/backend/src/routes/auth.ts` に `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/logout` を実装
    - `packages/backend/src/services/auth-service.ts` に bcrypt によるパスワードハッシュ化、JWT トークン生成・検証を実装
    - `packages/backend/src/middleware/auth.ts` に JWT 認証ミドルウェアを実装
    - Firestore の `users` コレクションにユーザーデータを保存
    - ログイン失敗5回でアカウントロック（15分間）のロジックを実装
    - _要件: 9.1, 9.2, 9.3, 9.4, 9.5_

  - [x] 2.2 プロパティテスト: 認証ラウンドトリップ (Property 11)
    - **Property 11: 認証ラウンドトリップ** — 有効なメールアドレスとパスワードで登録後、同じ認証情報でログインすると有効なトークンが返され、異なるパスワードでは拒否されることを検証
    - **検証対象: 要件 9.2, 9.3**

- [x] 3. 子供プロフィール API の実装
  - [x] 3.1 プロフィール CRUD API の実装
    - `packages/backend/src/routes/profiles.ts` に `POST /api/profiles`, `GET /api/profiles`, `GET /api/profiles/:id` を実装
    - `packages/backend/src/services/profile-service.ts` にプロフィールの作成・取得ロジックを実装
    - Zod スキーマによるリクエストバリデーションを適用
    - Firestore の `users/{userId}/childProfiles` サブコレクションにデータを保存
    - _要件: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 4. チェックポイント - 認証とプロフィール API の動作確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

- [x] 5. ストーリー生成エンジンの実装
  - [x] 5.1 ストーリーエンジンとコンテンツフィルターの実装
    - `packages/backend/src/services/story-engine.ts` に OpenAI GPT-5 を使用したストーリー生成ロジックを実装
    - 子供の名前を主人公名として使用するプロンプトテンプレートを作成
    - 対象年齢に応じた語彙・文章長の調整ロジックを実装
    - 8〜16ページのストーリー構造を生成するプロンプト設計
    - `packages/backend/src/services/content-filter.ts` にテキスト・画像の安全性チェックロジックを実装
    - 指数バックオフによるリトライ戦略（1秒、2秒、4秒、最大3回）を実装
    - _要件: 3.1, 3.2, 3.3, 3.4, 10.1, 10.3_

  - [ ]* 5.2 プロパティテスト: ストーリー生成 (Property 3, 4)
    - **Property 3: ストーリーに子供の名前が含まれる** — 任意のプロフィールとテーマに対して、生成されたストーリーの少なくとも1ページに子供の名前が含まれることを検証
    - **Property 4: ストーリーのページ数は8-16の範囲内** — 任意のプロフィールとテーマに対して、ページ数が8以上16以下であることを検証
    - **検証対象: 要件 3.1, 3.3**

  - [ ]* 5.3 プロパティテスト: コンテンツフィルター (Property 12)
    - **Property 12: コンテンツフィルターの一貫性** — 任意のテキストに対して、コンテンツフィルターの判定結果が冪等であることを検証
    - **検証対象: 要件 10.1, 10.3**

- [x] 6. イラスト生成エンジンの実装
  - [x] 6.1 イラストエンジンの実装
    - `packages/backend/src/services/illustration-engine.ts` に OpenAI gpt-image-1.5 を使用したイラスト生成ロジックを実装
    - 子供の外見特徴を反映したプロンプト生成ロジックを実装
    - 生成された画像を Firebase Storage (`users/{userId}/books/{bookId}/illustrations/page-{n}.png`) に保存
    - ページ単位のリトライ機能を実装
    - _要件: 4.1, 4.2, 4.3, 4.5_

  - [ ]* 6.2 プロパティテスト: イラスト数の一致 (Property 5)
    - **Property 5: イラスト数はストーリーのページ数と一致する** — 任意のストーリーに対して、生成されるイラスト数がページ数と等しいことを検証
    - **検証対象: 要件 4.1**

- [x] 7. 絵本生成 API と SSE 進捗通知の実装
  - [x] 7.1 絵本生成 API の実装
    - `packages/backend/src/routes/books.ts` に `POST /api/books/generate` を実装
    - ストーリー生成 → コンテンツチェック → イラスト生成の一連のパイプラインを構築
    - SSE (Server-Sent Events) による進捗通知（`story_generating`, `story_complete`, `illustration_generating`, `illustration_complete`, `complete`, `error`）を実装
    - 生成完了後に Firestore の `users/{userId}/books` サブコレクションにデータを保存
    - サムネイル画像の生成と Firebase Storage への保存を実装
    - _要件: 3.1, 3.5, 4.1, 4.4, 8.1_

  - [x] 7.2 絵本 CRUD API の実装
    - `packages/backend/src/routes/books.ts` に `GET /api/books`, `GET /api/books/:id`, `PUT /api/books/:id/pages/:pageNumber`, `DELETE /api/books/:id` を実装
    - テキスト編集時のコンテンツ安全性チェックを適用
    - 削除時に Firestore ドキュメントと Firebase Storage のファイルを両方削除
    - _要件: 6.2, 8.1, 8.2, 8.3, 8.5, 10.3, 10.4_

  - [ ]* 7.3 プロパティテスト: 絵本データの保存・取得・削除 (Property 10)
    - **Property 10: 絵本データの保存・取得・削除** — 任意の絵本データに対して、保存後に取得すると元のデータと同一であり、削除後に取得すると存在しないことを検証
    - **検証対象: 要件 8.1, 8.5**

- [x] 8. PDF 生成機能の実装
  - [x] 8.1 PDF レンダラーの実装
    - `packages/backend/src/services/pdf-renderer.ts` に pdf-lib を使用した PDF 生成ロジックを実装
    - 各ページにテキストとイラスト画像を配置するレイアウトロジックを実装
    - `GET /api/books/:id/download` エンドポイントを実装
    - 生成した PDF を Firebase Storage (`users/{userId}/books/{bookId}/output.pdf`) に保存
    - 最大2回のリトライ機能を実装
    - _要件: 7.1, 7.2, 7.3, 7.4_

  - [ ]* 8.2 プロパティテスト: PDF 生成の正確性 (Property 9)
    - **Property 9: PDF 生成の正確性** — 任意の有効な絵本データに対して、PDF レンダラーが有効な PDF バイナリを生成し、ページ数が絵本のページ数と一致することを検証
    - **検証対象: 要件 7.1, 7.2**

- [x] 9. チェックポイント - バックエンド API の統合確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

- [x] 10. フロントエンド基盤のセットアップ
  - [x] 10.1 React アプリケーションの初期設定
    - Vite + React + TypeScript プロジェクトを `packages/frontend/` に作成
    - TailwindCSS の設定
    - API クライアント (`packages/frontend/src/api/client.ts`) の作成（JWT トークン付きリクエスト、エラーハンドリング）
    - ルーティング設定（React Router）
    - 認証状態管理（Context API）
    - _要件: 全体_

- [x] 11. 認証画面の実装
  - [x] 11.1 ログイン・アカウント登録画面の実装
    - `LoginPage` コンポーネントの実装（ログインフォーム、登録フォームの切り替え）
    - Zod スキーマによるクライアントサイドバリデーション
    - 認証エラー・アカウントロックのメッセージ表示
    - ログイン成功後のダッシュボードへのリダイレクト
    - ログアウト機能の実装
    - _要件: 9.1, 9.2, 9.3, 9.4, 9.5_

- [x] 12. ダッシュボードと子供プロフィール画面の実装
  - [x] 12.1 ダッシュボード画面の実装
    - `DashboardPage` コンポーネントの実装（保存済み絵本一覧のサムネイル付き表示）
    - 絵本選択時のプレビュー画面への遷移
    - 絵本削除機能（確認ダイアログ付き）
    - _要件: 8.2, 8.3, 8.4, 8.5_

  - [x] 12.2 子供プロフィール入力画面の実装
    - `ProfileFormPage` コンポーネントの実装（名前、年齢、性別、好きな色、好きな動物、外見の特徴の入力フォーム）
    - Zod スキーマによるクライアントサイドバリデーションとインラインエラーメッセージ表示
    - プロフィール保存後のテーマ選択画面への遷移
    - _要件: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 13. テーマ選択と絵本生成画面の実装
  - [x] 13.1 テーマ選択画面の実装
    - `ThemeSelectPage` コンポーネントの実装（6つのテーマをカード形式で表示）
    - テーマ未選択時の警告メッセージ表示
    - テーマ選択後の生成開始ボタン
    - _要件: 2.1, 2.2, 2.3_

  - [x] 13.2 生成中画面の実装
    - `GeneratingPage` コンポーネントの実装
    - SSE による進捗状況のリアルタイム表示（`ProgressBar` コンポーネント）
    - ストーリー生成中、イラスト生成中（ページ単位）の進捗表示
    - エラー発生時の再試行ボタン表示
    - 生成完了後のプレビュー画面への自動遷移
    - _要件: 3.4, 3.5, 4.4, 4.5_

- [x] 14. 絵本プレビュー・編集画面の実装
  - [x] 14.1 絵本プレビュー画面の実装
    - `PreviewPage` コンポーネントの実装
    - `BookViewer` コンポーネントの実装（テキストとイラストの統合表示、ページめくり機能）
    - 現在のページ番号と総ページ数の表示
    - ダウンロードボタン（PDF 生成 API 呼び出し、生成中メッセージ表示）
    - _要件: 5.1, 5.2, 5.3, 5.4, 7.1, 7.3_

  - [ ]* 14.2 プロパティテスト: ページナビゲーション (Property 6)
    - **Property 6: ページナビゲーションの正確性** — 任意の N ページの絵本に対して、次ページ操作でページ番号が1増加（最大N）、前ページ操作で1減少（最小1）し、表示が正確であることを検証
    - **検証対象: 要件 5.2, 5.3**

  - [x] 14.3 テキスト編集機能の実装
    - `PageEditor` コンポーネントの実装（インライン編集、200文字制限の警告表示）
    - 編集保存時の API 呼び出しとコンテンツ安全性チェック結果の表示
    - 編集取り消し機能（元のテキストへの復元）
    - 不適切コンテンツ検出時の通知と修正促進メッセージ
    - _要件: 6.1, 6.2, 6.3, 6.4, 10.3, 10.4_

  - [ ]* 14.4 プロパティテスト: テキスト編集 (Property 7, 8)
    - **Property 7: テキスト編集ラウンドトリップ** — 任意の200文字以内のテキストに対して、編集保存後に取得すると同一データが返されることを検証
    - **Property 8: 編集取り消しによる原文復元** — 任意のページに対して、編集後に取り消すと元のテキストに戻ることを検証
    - **検証対象: 要件 6.2, 6.3**

- [x] 15. チェックポイント - フロントエンド統合確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

- [x] 16. エラーハンドリングと統合仕上げ
  - [x] 16.1 共通エラーハンドリングの実装
    - バックエンドの共通エラーハンドリングミドルウェア (`ApiError` 形式のレスポンス) を実装
    - フロントエンドのグローバルエラーハンドリング（ネットワークエラー、認証エラー時のリダイレクト）を実装
    - API クライアントにリトライロジックを追加
    - _要件: 3.4, 4.5, 7.4_

- [x] 17. 最終チェックポイント - 全テスト通過確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

## 備考

- `*` マーク付きのタスクはオプションであり、MVP を早く完成させるためにスキップ可能
- 各タスクは特定の要件を参照しており、トレーサビリティを確保
- チェックポイントで段階的に動作を検証
- プロパティテストは設計ドキュメントの正確性プロパティに基づく普遍的な検証
- ユニットテストは特定のエッジケースやエラー条件の検証に使用
