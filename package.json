{
  "name": "personalized-picture-book",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "build": "npm run build --workspaces",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.1",
    "fast-check": "^4.1.1"
  }
}
